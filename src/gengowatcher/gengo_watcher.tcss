/* Theme aliases: these map directly to the currently selected Textual theme. */
$surface-muted: $border-blurred;
$accent-blue: $primary;
$accent-green: $success;
$accent-red: $error;
$accent-orange: $warning;
$accent-yellow: $warning-lighten-1;
$accent-violet: $secondary;
$accent-aqua: $accent;
$accent-pink: $accent-muted;

Screen {
    color: $text;
    background: $background;
}

/* ============================================================================
   Layout Components
   ============================================================================ */

/* Title Bar - seamless, no box */
TitleBar {
    height: 3;
    dock: top;
    layout: vertical;
    align: center middle;
    border: none;
}

.brand {
    text-style: bold;
    color: $accent-blue;
}

.separator {
    color: $surface-muted;
    display: none;
}

.info-row {
    width: 100%;
    align: center middle;
    color: $text-muted;
}

.config-info {
    color: $text-muted;
}

.dim {
    color: $surface-muted;
}

/* Metrics Row */
MetricsRow {
    height: 4;
    margin: 0 1;
    align: center middle;
    layout: horizontal;
}

MetricCard {
    width: 1fr;
    height: 100%;
    background: $panel;
    border: round $surface-muted;
    layout: vertical;
    align: center middle;
    margin: 0 1;
    padding: 0 1;
}

/* Card accents from current theme roles */
MetricCard.found { 
    border-title-color: $accent-green;
    color: $accent-green;
}
MetricCard.accepted { 
    border-title-color: $accent-aqua;
    color: $accent-aqua;
}
MetricCard.value { 
    border-title-color: $accent-yellow;
    color: $accent-yellow;
}
MetricCard.rate { 
    border-title-color: $accent-orange;
    color: $accent-orange;
}
MetricCard.today { 
    border-title-color: $accent-violet;
    color: $accent-violet;
}

MetricCard:focus {
    border: round $accent-blue;
}

MetricCard.found:focus { 
    border-title-color: $accent-green;
    color: $accent-green;
}
MetricCard.accepted:focus { 
    border-title-color: $accent-aqua;
    color: $accent-aqua;
}
MetricCard.value:focus { 
    border-title-color: $accent-yellow;
    color: $accent-yellow;
}
MetricCard.rate:focus { 
    border-title-color: $accent-orange;
    color: $accent-orange;
}
MetricCard.today:focus { 
    border-title-color: $accent-violet;
    color: $accent-violet;
}

.metric-grid {
    layout: grid;
    grid-size: 2;
    grid-columns: auto 1fr;
    width: 100%;
    height: 1;
    align: center middle;
}

.metric-icon {
    color: $text;
    margin-right: 1;
}

.metric-value {
    text-style: bold;
    color: $text;
    width: 100%;
    text-align: right;
}

.metric-label {
    width: 100%;
    text-align: center;
    color: $text-muted;
    padding-bottom: 0;
}

/* Status Row - seamless, matches surface */
StatusRow {
    height: 3;
    width: 100%;
    layout: horizontal;
    align: center middle;
    margin: 0 1 1 1;
    padding: 0 2;
    border: round $surface-muted;
}

StatusIndicator {
    layout: horizontal;
    margin: 0 2;
    min-width: 8;
    align: center middle;
    width: 1fr;
}

.status-label {
    width: auto;
    color: $text-muted;
}

/* Status Colors */
.status-live { 
    color: $accent-green; 
    text-style: bold;
}
.status-working { 
    color: $accent-orange; 
}
.status-error { 
    color: $accent-red; 
    text-style: bold;
}
.status-stale {
    color: $accent-yellow;
    text-style: bold;
}
.status-idle { 
    color: $text-muted; 
}
.status-disabled {
    color: $text-muted;
}

.status-live .status-label {
    color: $accent-green;
    text-style: bold;
}
.status-working .status-label { color: $accent-orange; }
.status-error .status-label { color: $accent-red; }
.status-stale .status-label { color: $accent-yellow; }
.status-idle .status-label { color: $text-muted; }
.status-disabled .status-label { color: $text-muted; }

.status-icon {
    margin-right: 1;
}

/* Dashboard Grid */
.dashboard-grid {
    layout: grid;
    grid-size: 2 2;
    grid-columns: 1fr 1fr;
    grid-rows: 1fr 1fr;
    margin: 0 1;
    height: 1fr;
    min-height: 20;
}

/* Panel styling - unified with surface */
.dashboard-panel {
    background: $panel;
    border: round $surface-muted;
    padding: 0 1;
    height: 100%;
    margin: 0 1 1 1;
    border-title-color: $accent-blue;
    border-title-background: $panel;
}

/* Specific panel accents */
.dashboard-panel.jobs { border-title-color: $accent-blue; }
.dashboard-panel.chart { border-title-color: $accent-green; }
.dashboard-panel.session { border-title-color: $accent-orange; }
.dashboard-panel.sources { border-title-color: $accent-red; }

/* Activity log at bottom */
.activity-panel {
    background: $panel;
    border: round $surface-muted;
    padding: 0 1;
    height: 1fr;
    min-height: 6;
    margin: 0 1 1 1;
}

.activity-panel .panel-title {
    color: $accent-blue;
    background: $panel;
    text-style: bold;
    dock: top;
    padding: 0 1;
}

DashboardQuadrant {
    border: round $surface-muted;
    background: $panel;
    margin: 0 1 1 1;
    padding: 0 1;
    height: 100%;
    border-title-color: $accent-blue;
}

.quadrant-title {
    background: $panel;
    color: $accent-blue;
    dock: top;
    padding: 0 1;
}

/* Specific Quadrants */
JobsPreview {
    height: 100%;
    border-title-color: $accent-blue;
}

ActivityPreview {
    height: 8;
    border-title-color: $accent-aqua;
}

ChartPlaceholder {
    border-title-color: $accent-green;
}

JobsHourChart {
    border-title-color: $accent-green;
}

.chart-ascii {
    padding: 0 1;
    height: 100%;
}

ConfigPreview {
    border-title-color: $accent-violet;
}

.config-scroll {
    width: 100%;
    height: 100%;
    overflow-x: auto;
    overflow-y: auto;
    scrollbar-color: $surface-muted;
}

.config-display {
    width: auto;
    min-width: 100%;
}

SessionStats {
    border-title-color: $accent-orange;
}

TelemetryPanel {
    border-title-color: $accent-red;
}

TelemetryTab {
    padding: 1 2;
}

RichLog {
    border: none;
    scrollbar-color: $surface-muted;
}

DataTable {
    border: none;
    height: 100%;
}

JobsPreview {
    height: 1fr;
}

DataTable > .datatable--header {
    color: $accent-blue;
}

/* Tabs */
TabbedContent {
    height: 1fr;
}

TabPane {
    padding: 0;
}

Tabs {
    dock: top;
}

Tab {
    color: $text-muted;
}

Tab.-active {
    color: $text;
    text-style: bold;
}

Underline {
    color: $accent-blue;
}

/* Footer & Input */
Input {
    dock: bottom;
    height: 3;
    border: round $surface-muted;
}

Footer {
    dock: bottom;
    color: $text-muted;
}

FooterKey {
    color: $text;
}

FooterKey > .footer-key--key {
    color: $accent-blue;
}
